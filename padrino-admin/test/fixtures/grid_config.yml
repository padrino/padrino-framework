renderer:
  boolean: | __js__
    function(v, p, r){ 
      var tmp = (v!=undefined && v!='');
      return Ext.util.Format.boolRenderer(tmp, p, r) 
    }

bar: Yea!!

nesting:
  example: %show

columns:
  - id: custom_id
  - id: paid_time
    align: center
    renderer: %boolean
    editor: %checkbox
    nesting: %example
    test: <%= @my_var %>

editor:
  checkbox:
    xtype: checkbox

example:
  show: %%foo

foo: %%bar
